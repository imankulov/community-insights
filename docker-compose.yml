version: "2.1"
services:
  mysql:
    image: "mysql:5.6"
    ports:
      - "127.0.0.1:3306:3306"
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    volumes:
      - ./mysql/data:/var/lib/mysql:delegated
